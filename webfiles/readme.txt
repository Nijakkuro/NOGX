Place your web files here if needed.
The NOGX extension will include them in the builds.
You can also use your custom 'index.html' file located here, but it should be based on the default 'index.html' file from the extension.
If you use wasm compression, keep the NOGX_CODE_COMPRESSION_START/END markers from the default index.html around the fflate/instantiateWasm block.
